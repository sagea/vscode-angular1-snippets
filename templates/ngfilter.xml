<snippet>
    <prefix>ngfilter</prefix>
    <description>Angular 1.x Filter</description>
    <displayName>Angular Filter</displayName>
    <body>
        <![CDATA[
        (function(){
    'use strict';

    angular
        .module('${Module}')
        .filter('${Filter}', ${Filter})

    function ${Filter}(){

        return ${Filter}Fn;

        function ${Filter}Fn(${Params}){
            $0
            return ${Params}
        }
    }

}());
        ]]>
    </body>
</snippet>
