<snippet>
    <prefix>ng1xdirective</prefix>
    <displayName>Angular Directive</displayName>
    <description>Angular 1.x Directive</description>
    <body>
        <![CDATA[
        (function () {
    'use strict';

    angular
        .module ('${Module}')
        .directive ('${directive}', ${directive});


    function ${directive}(${Dependencies}) {

        function ${Controller}Controller(){
            var vm = this;
            $0
            init();

            function init(){

            }
        }

        function link(){

        }

        return {
            bindToController: true,
            controller: ${Controller}Controller,
            controllerAs: '${Ctrl}',
            link: link,
            restrict: '${AE}',
            scope: {},
        }
    }

} ());
        ]]>
    </body>
</snippet>